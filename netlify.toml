[build]
  command = "npm install && npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22.13.0"

[functions]
  node_bundler = "esbuild"
  included_files = ["dist/server/**"]
